# Few-Shot Example Guidance

Few-shot examples are most useful when they demonstrate the exact artifact format expected by downstream validators.

Good examples show concise input context, the target output structure, preferred tone and fidelity, and common failure cases to avoid.
