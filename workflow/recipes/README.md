# workflow/recipes/

- `full-sdlc-v2.yaml` — canonical end-to-end software delivery flow.
- `python-project.yaml` — focused Python project workflow.
- `requirements-gathering.yaml` — product vision and requirements flow.
- `code-review.yaml` — code and security review flow.
- `architecture-review.yaml` — architecture design review flow.
- `archived/` — historical recipes retained for reference only.
